require 'spec_helper'

describe Stockfighter do
  it 'has a version number' do
    expect(Stockfighter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
